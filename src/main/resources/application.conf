ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [com.suru.ApplicationKt.module]
  }
}

database {
  url = "jdbc:postgresql://localhost:9630/students"
  url = ${?DATABASE_URL}
  user = "student_user"
  user = ${?DATABASE_USER}
  password = "student_password"
  password = ${?DATABASE_PASSWORD}
}